extends Node


signal enemy_attack(enemy_damage)

signal enemy_attack_buildings(enemy_damage)

signal enemy_died(enemy_position)

signal day_time(state, day_count)

signal spawner_dead(position_x)

signal enemy_spawned()

signal spawn_enemy(count, day_count)

signal light_is_loaded(state)
